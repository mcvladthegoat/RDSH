<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/rdshBlue</item>
        <item name="colorPrimaryDark">@color/rdshDarkBlue</item>
        <item name="colorAccent">@color/rdshLightRed</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="RdshCheckBox" parent="Theme.AppCompat.Light">
        <item name="colorControlNormal">@color/rdshLightRed</item>
        <item name="colorControlActivated">@color/rdshDarkRed</item>
    </style>

    <style name="AddEventTitle" parent="@style/Base.TextAppearance.AppCompat.Title">
        <item name="android:textSize">15sp</item>
    </style>

</resources>
